services:
  - type: web
    name: qwen-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: QWEN_API_KEY
        value: your_qwen_api_key_here
